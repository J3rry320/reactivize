import React from 'react';
import ReactDOM from 'react-dom';

{{#if useRedux}}import { Provider } from 'react-redux';
import store from './store';{{/if}}
import App from './components/App';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    {{#if useRedux}}<Provider store={store}>{{/if}}
     
        <App />

    {{#if useRedux}}</Provider>{{/if}}
  </React.StrictMode>,
  document.getElementById('root')
);
